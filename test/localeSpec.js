describe( "locale", function() {

	var i18n = babelbox;


	beforeEach( function() {
		i18n.reset();
	} );

	it( "get\'s locale from url if present", function() {
		//expect( i18n.tokens ).toEqual( {} );
	} );

	it( "get\'s locale from cookie if not in url", function() {
		//expect( i18n.tokens ).toEqual( {} );
	} );

	it( "get\'s locale from browser if not in cookie", function() {
		//expect( i18n.tokens ).toEqual( {} );
	} );

	it( "gets locale from custom source if provided", function() {
		//expect( i18n.tokens ).toEqual( {} );
	} );

	it( "saves locale in cookie when set", function() {
		//expect( i18n.tokens ).toEqual( {} );
	} );

	it( "saves locale in custom source when set", function() {
		//expect( i18n.tokens ).toEqual( {} );
	} );

	it( "emits event when locale is changed if eventHub is set", function() {
		//expect( i18n.tokens ).toEqual( {} );
	} );

} );
